<UserControl x:Class="Source_MFC.Views.UsCtrl_DevCont"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Source_MFC.Views"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="25" d:DesignWidth="70">
    <Grid>
        <StackPanel Orientation="Horizontal" Height="25" Width="70">
            <materialDesign:PackIcon x:Name="Icon_Dev" Kind="{Binding b_IconCont}" Height="18" Width="18" Background="{x:Null}" BorderBrush="{x:Null}" Foreground="{Binding b_IconForeGround}" VerticalAlignment="Center"/>
            <TextBlock x:Name="txt_Dev" Text="{Binding b_DevName}" TextAlignment="Center" Foreground="{Binding b_devNameforegroud}" Width="52" VerticalAlignment="Center"/>
        </StackPanel>
    </Grid>
</UserControl>
